# Radio Button

Kontrolle wird auf null gesetzt -> keine Option ist ausgewählt

```js
function defaultRadios(fieldsetGuid) {
  getElement(fieldsetGuid).oUp.radioElements.forEach((radiocontrol) =>
    Browser.setValue(radiocontrol, "")
  );
  return true;
}
```
